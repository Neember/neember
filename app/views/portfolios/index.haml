#portfolio-container
  .row
    %h1 PORTFOLIOS

  - @portfolios.each do |portfolio|
    .row
      .portfolio
        .image
          %a{href:"#{portfolio.url}", target:"blank"}
            %img{src:"#{image_url("/portfolios/#{portfolio.image}")}"}
        .info
          .name
            %a{href:"#{portfolio.url}", target:"blank"}= portfolio.name
          .description= portfolio.description.html_safe

      .clearfix
